import { MinOptionsRequiredDirective } from './min-options-required.directive';

describe('MimOptionsRequiredDirective', () => {
  it('should create an instance', () => {
    const directive = new MinOptionsRequiredDirective();
    expect(directive).toBeTruthy();
  });
});
