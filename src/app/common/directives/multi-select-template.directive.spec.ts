import { MultiSelectTemplateDirective } from './multi-select-template.directive';

describe('MultiSelectTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiSelectTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
